<% if user_signed_in? %>
  <header class="navbar navbar-default">
    <div class="container-fluid">
      <nav>
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>          
        </div>
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
          <ul class="nav navbar-nav text-center">            
              <% if current_user.is_admin? %>
                <% if $total_revised.present? and $total_revised > 0 %>
                  <li>
                    <span class="alert-n"><%= $total_revised %></span>
                  </li>
                <% end %>
                <li>                  
                  <%= link_to "Solicitudes", contacts_path, :class => 'navbar-link' %>
                </li>
                <li>
                  <%= link_to "Clientes", user_list_path, :class => 'navbar-link' %>
                </li>
                <li>
                  <%= link_to "Servicios", dailys_index_path, :class => 'navbar-link' %>
                </li>
                <li>
                  <%= link_to "Reportes", users_show_path, :class => 'navbar-link' %>
                </li>
                <li>
                  <%= link_to "Contenido", articles_path, :class => 'navbar-link' %>
                </li>  
                <li>
                  <%= link_to "Marcas", brands_path, :class => 'navbar-link' %>
                </li> 
                <li>
                  <%= link_to "Tipos", types_path, :class => 'navbar-link' %>
                </li> 
              <% end %>
              <li>
                <%= link_to "Cerrar sesión", destroy_user_session_path, method: :delete, :class => 'navbar-link'  %>
              </li>
          </ul> 
        </div>
      </nav>
    </div>
  </header>
<div class="container">
  <div class="col-xs-12">
    <%= image_tag('logo-mg.png', style: 'padding: 1em; max-width: 200px;') %>
  </div>
</div>
<% end %>
